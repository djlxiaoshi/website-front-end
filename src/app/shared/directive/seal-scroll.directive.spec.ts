import { SealScrollDirective } from './seal-scroll.directive';

describe('SealScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SealScrollDirective();
    expect(directive).toBeTruthy();
  });
});
